# FM-LLM Solver - Main Requirements
# This installs the base dependencies needed for all components
# For specific use cases, install additional requirement files:
#
# Web Interface:    pip install -r requirements/base.txt -r requirements/web.txt
# ML/Inference:     pip install -r requirements/base.txt -r requirements/inference.txt  
# Development:      pip install -r requirements/base.txt -r requirements/dev.txt
# Production:       pip install -r requirements/base.txt -r requirements/web.txt -r requirements/production.txt

-r requirements/base.txt 