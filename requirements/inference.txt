# FM-LLM Solver - Inference & ML Requirements
# Machine learning and LLM dependencies
# Install with: pip install -r base.txt -r inference.txt

# PyTorch (install separately with CUDA support if needed)
# pip install torch>=2.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0

# Transformers & LLM Framework
transformers>=4.35.0
accelerate>=0.24.0
bitsandbytes>=0.41.0
einops>=0.7.0
sentencepiece>=0.1.99
protobuf>=3.20.0
safetensors>=0.4.0
huggingface_hub>=0.19.0

# Fine-tuning
peft>=0.9.0
datasets>=2.14.0
trl>=0.7.0

# RAG / Knowledge Base
faiss-cpu>=1.7.4  # Use faiss-gpu for GPU support
sentence-transformers>=2.2.0
langchain>=0.1.0
langchain-community>=0.1.0

# Optimization & Verification
cvxpy>=1.4.0
mosek>=10.1.0  # Optional: commercial solver

# API Framework
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6 